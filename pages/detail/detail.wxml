<!--detail.wxml-->
<view class="container">
    <view class="art-header">
        <image src="{{art.image}}" class="art-img"/>
        <view class="art-title">{{art.title}}</view>
        <view class="art-source">{{art.image_source}}</view>
    </view>
    <prev class="art-content">
      {{art.body}}
    </prev> 
</view>